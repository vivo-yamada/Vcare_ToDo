@model VcareTodo.Models.ViewModels.TodoViewModel
@{
    ViewData["Title"] = "ToDo管理";
}

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Vcare ToDo管理</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f8faff 0%, #e8f2ff 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .header-bar {
            background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
            color: white;
            padding: 15px 0;
            margin-bottom: 20px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        .unscheduled-tasks {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 12px rgba(33, 150, 243, 0.1);
            padding: 20px;
            margin-bottom: 20px;
            min-height: 200px;
        }
        .unscheduled-tasks h5 {
            color: #1976d2;
            font-weight: bold;
            padding-left: 10px;
            border-left: 4px solid #2196f3;
        }
        .unscheduled-tasks.drop-zone {
            background: rgba(33, 150, 243, 0.1) !important;
            box-shadow: inset 0 0 0 3px #2196f3;
        }
        .calendar-container {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 12px rgba(33, 150, 243, 0.1);
            padding: 20px;
        }
        .calendar-container h5 {
            color: #1976d2;
            font-weight: bold;
            padding-left: 10px;
            border-left: 4px solid #2196f3;
        }
        .task-card {
            background: #e3f2fd;
            border-left: 4px solid #2196f3;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
            cursor: move;
            transition: all 0.3s ease;
        }
        .task-card:hover {
            box-shadow: 0 5px 15px rgba(33, 150, 243, 0.4);
            transform: translateY(-2px);
        }
        .task-card.important {
            background: #fff3cd;
            border-left-color: #ffc107;
        }
        .task-card.important:hover {
            box-shadow: 0 5px 15px rgba(255, 193, 7, 0.4);
        }
        /* Time-based Calendar Styles */
        .calendar-grid {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
            border: 1px solid #e3f2fd;
            border-radius: 6px;
            overflow: hidden;
        }

        /* Calendar Header */
        .calendar-header {
            display: grid;
            background: white;
            border-bottom: 2px solid #e3f2fd;
            position: sticky;
            top: 0;
            z-index: 10;
        }
        .calendar-header.day-mode {
            grid-template-columns: 80px 1fr;
        }
        .calendar-header.week-mode {
            grid-template-columns: 80px repeat(7, 1fr);
        }
        .calendar-header.month-mode {
            grid-template-columns: repeat(7, 1fr);
        }
        .header-cell {
            padding: 12px;
            text-align: center;
            font-weight: bold;
            color: #1976d2;
            border-right: 1px solid #e3f2fd;
        }
        .header-cell:last-child {
            border-right: none;
        }
        .header-cell.today {
            background: #e3f2fd;
            color: #1976d2;
        }

        /* Calendar Body */
        .calendar-body {
            display: grid;
            background: white;
            position: relative;
        }
        .calendar-body.day-mode,
        .calendar-body.week-mode {
            grid-template-columns: 80px 1fr;
            min-height: 600px;
        }
        .calendar-body.month-mode {
            grid-template-columns: repeat(7, 1fr);
            grid-auto-rows: 120px;
        }

        /* Time Column */
        .time-column {
            border-right: 2px solid #e3f2fd;
        }
        .time-slot {
            height: 60px;
            border-bottom: 1px solid #e3f2fd;
            padding: 5px;
            font-size: 0.85em;
            color: #666;
            text-align: right;
            padding-right: 8px;
        }

        /* Day Columns */
        .day-columns {
            display: grid;
            position: relative;
        }
        .day-columns.single {
            grid-template-columns: 1fr;
        }
        .day-columns.week {
            grid-template-columns: repeat(7, 1fr);
        }
        .day-column {
            border-right: 1px solid #e3f2fd;
            position: relative;
        }
        .day-column:last-child {
            border-right: none;
        }

        /* Time Grid Lines */
        .time-grid {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
        }
        .time-grid-line {
            height: 60px;
            border-bottom: 1px solid #f0f0f0;
            transition: background-color 0.2s ease;
        }
        .time-grid-line.drop-zone {
            background: rgba(33, 150, 243, 0.3) !important;
            box-shadow: inset 0 0 0 2px #2196f3;
        }

        /* Month View Cells */
        .month-cell {
            border: 1px solid #e3f2fd;
            padding: 5px;
            position: relative;
            background: #f8faff;
        }
        .month-cell.today {
            background: #e3f2fd;
        }
        .month-cell.other-month {
            background: #f5f5f5;
            color: #999;
        }
        .month-date {
            font-weight: bold;
            color: #1976d2;
            margin-bottom: 5px;
        }

        /* Task Card in Calendar */
        .calendar-task {
            position: absolute;
            left: 2px;
            right: 2px;
            background: #2196f3;
            color: white;
            border-radius: 4px;
            padding: 4px 6px;
            font-size: 0.85em;
            cursor: pointer;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            z-index: 1;
        }
        .calendar-task:hover {
            background: #1976d2;
            box-shadow: 0 2px 8px rgba(33, 150, 243, 0.4);
        }
        .calendar-task.important {
            background: #ffc107;
            color: #333;
        }
        .calendar-task.important:hover {
            background: #ffb300;
            box-shadow: 0 2px 8px rgba(255, 193, 7, 0.4);
        }

        /* Drop zones */
        .drop-zone {
            background: #e3f2fd !important;
            box-shadow: inset 0 0 0 2px #2196f3;
        }
        .view-controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .task-title {
            font-weight: bold;
            margin-bottom: 5px;
        }
        .task-meta {
            font-size: 0.8em;
            color: #666;
        }
        .add-task-btn {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, #2196f3 0%, #1976d2 100%);
            border: none;
            color: white;
            font-size: 24px;
            box-shadow: 0 8px 24px rgba(33, 150, 243, 0.15);
            z-index: 1000;
            transition: all 0.3s ease;
        }
        .add-task-btn:hover {
            transform: translateY(-3px) scale(1.1);
            box-shadow: 0 12px 28px rgba(33, 150, 243, 0.3);
        }
        @@media (max-width: 768px) {
            .view-controls {
                flex-direction: column;
                align-items: stretch;
                gap: 10px;
            }
            .task-card {
                font-size: 0.9em;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header-bar">
        <div class="container-fluid">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h4 class="mb-0">
                        <i class="fas fa-tasks me-2"></i>
                        Vcare ToDo管理
                    </h4>
                </div>
                <div class="col-md-6 text-end">
                    <span class="me-3">ようこそ、@Model.CurrentUserName さん</span>
                    <form asp-action="Logout" asp-controller="Account" method="post" class="d-inline">
                        <button type="submit" class="btn btn-outline-light btn-sm">
                            <i class="fas fa-sign-out-alt me-1"></i>ログアウト
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <!-- View Controls -->
        <div class="view-controls">
            <div class="d-flex align-items-center gap-3">
                <!-- View Mode Buttons -->
                <div class="btn-group" role="group">
                    <input type="radio" class="btn-check" name="viewMode" id="dayView" value="day" @(Model.ViewMode == "day" ? "checked" : "")>
                    <label class="btn btn-outline-primary" for="dayView">日</label>

                    <input type="radio" class="btn-check" name="viewMode" id="weekView" value="week" @(Model.ViewMode == "week" ? "checked" : "")>
                    <label class="btn btn-outline-primary" for="weekView">週</label>

                    <input type="radio" class="btn-check" name="viewMode" id="monthView" value="month" @(Model.ViewMode == "month" ? "checked" : "")>
                    <label class="btn btn-outline-primary" for="monthView">月</label>
                </div>

                <!-- Date Navigation -->
                <div class="d-flex align-items-center">
                    <button class="btn btn-outline-secondary btn-sm me-2" onclick="navigateDate(-1)">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <span id="currentDateDisplay" class="fw-bold">@Model.CurrentDate.ToString("yyyy年MM月dd日")</span>
                    <button class="btn btn-outline-secondary btn-sm ms-2" onclick="navigateDate(1)">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <!-- User Selector -->
            <div>
                <select class="form-select" id="userSelector" onchange="changeUser()">
                    @foreach (var employee in Model.Employees)
                    {
                        if (employee.コード == Model.SelectedUserCode)
                        {
                            <option value="@employee.コード" selected>@employee.氏名</option>
                        }
                        else
                        {
                            <option value="@employee.コード">@employee.氏名</option>
                        }
                    }
                </select>
            </div>
        </div>

        <div class="row">
            <!-- Unscheduled Tasks -->
            <div class="col-lg-4">
                <div class="unscheduled-tasks">
                    <h5 class="mb-3">
                        <i class="fas fa-list me-2"></i>
                        未定タスク (@Model.UnscheduledTasks.Count)
                    </h5>
                    <div id="unscheduledTasksList">
                        @foreach (var task in Model.UnscheduledTasks)
                        {
                            <div class="task-card @(task.重要タスクFLG == true ? "important" : "")" draggable="true" data-task-id="@task.ID" onclick="editTask(@task.ID)">
                                <div class="task-title">@task.内容</div>
                                <div class="task-meta">
                                    @if (!string.IsNullOrEmpty(task.取引先名))
                                    {
                                        <span class="badge bg-secondary me-1">@task.取引先名</span>
                                    }
                                    @if (task.見積工数.HasValue)
                                    {
                                        <small>予定: @(task.見積工数)h</small>
                                    }
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- Calendar -->
            <div class="col-lg-8">
                <div class="calendar-container">
                    <h5 class="mb-3">
                        <i class="fas fa-calendar me-2"></i>
                        カレンダー
                    </h5>
                    <div id="calendarGrid" class="calendar-grid">
                        <!-- Calendar will be generated by JavaScript -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Add Task Button -->
    <button class="add-task-btn" onclick="addNewTask()" title="新しいタスクを追加">
        <i class="fas fa-plus"></i>
    </button>

    <!-- Task Detail Modal -->
    <div class="modal fade" id="taskModal" tabindex="-1" aria-labelledby="taskModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="taskModalLabel">タスク詳細</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="taskForm">
                        <input type="hidden" id="taskId" name="ID" />

                        <div class="row">
                            <!-- 基本情報 -->
                            <div class="col-12 mb-3">
                                <label for="taskContent" class="form-label">内容 <span class="text-danger">*</span></label>
                                <textarea class="form-control" id="taskContent" name="内容" rows="3" required></textarea>
                            </div>

                            <!-- 取引先情報 -->
                            <div class="col-md-6 mb-3">
                                <label for="customerCode" class="form-label">取引先コード</label>
                                <input type="text" class="form-control" id="customerCode" name="取引先コード" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="customerName" class="form-label">取引先名</label>
                                <input type="text" class="form-control" id="customerName" name="取引先名" />
                            </div>

                            <!-- 工数情報 -->
                            <div class="col-md-4 mb-3">
                                <label for="estimatedHours" class="form-label">予定工数</label>
                                <input type="number" step="0.1" class="form-control" id="estimatedHours" name="見積工数" />
                            </div>
                            <div class="col-md-4 mb-3">
                                <label for="actualHours" class="form-label">実績工数</label>
                                <input type="number" step="0.1" class="form-control" id="actualHours" name="実績工数" />
                            </div>

                            <!-- 作業予定日・時刻 -->
                            <div class="col-md-4 mb-3">
                                <label for="scheduledDate" class="form-label">作業予定日</label>
                                <input type="date" class="form-control" id="scheduledDate" name="作業予定日" />
                            </div>
                            <div class="col-md-4 mb-3">
                                <label for="startTime" class="form-label">開始時刻</label>
                                <input type="time" class="form-control" id="startTime" name="開始時刻" />
                            </div>
                            <div class="col-md-4 mb-3">
                                <label for="endTime" class="form-label">終了時刻</label>
                                <input type="time" class="form-control" id="endTime" name="終了時刻" />
                            </div>

                            <div class="col-md-6 mb-3">
                                <label for="assignee" class="form-label">担当者</label>
                                <select class="form-select" id="assignee" name="担当者">
                                    @foreach (var employee in Model.Employees)
                                    {
                                        if (employee.コード == Model.CurrentUserCode)
                                        {
                                            <option value="@employee.コード" selected>@employee.氏名</option>
                                        }
                                        else
                                        {
                                            <option value="@employee.コード">@employee.氏名</option>
                                        }
                                    }
                                </select>
                            </div>

                            <!-- 重要タスク -->
                            <div class="col-md-6 mb-3">
                                <div class="form-check mt-4">
                                    <input class="form-check-input" type="checkbox" id="importantTask" name="重要タスクFLG" style="transform: scale(1.5);">
                                    <label class="form-check-label ms-2" for="importantTask" style="font-weight: bold; color: #ffc107;">
                                        <i class="fas fa-star me-1"></i>重要タスク
                                    </label>
                                </div>
                            </div>

                            <!-- 備考 -->
                            <div class="col-12 mb-3">
                                <label for="remarks" class="form-label">備考</label>
                                <textarea class="form-control" id="remarks" name="備考" rows="3"></textarea>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger" id="deleteTaskBtn" onclick="deleteTask()" style="display: none;">削除</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">キャンセル</button>
                    <button type="button" class="btn btn-primary" onclick="saveTask()">保存</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        let currentDate = new Date('@Model.CurrentDate.ToString("yyyy-MM-dd")');
        let viewMode = '@Model.ViewMode';
        let selectedUserCode = '@Model.SelectedUserCode';

        // Initialize calendar on page load
        $(document).ready(function() {
            generateCalendar();
            setupDragAndDrop();
            loadScheduledTasks();
        });

        // Generate calendar based on view mode
        function generateCalendar() {
            const calendarGrid = document.getElementById('calendarGrid');
            calendarGrid.innerHTML = '';

            if (viewMode === 'day') {
                generateDayView();
            } else if (viewMode === 'week') {
                generateWeekView();
            } else if (viewMode === 'month') {
                generateMonthView();
            }
        }

        // Day View: Single day with time slots
        function generateDayView() {
            const calendarGrid = document.getElementById('calendarGrid');
            const dayNames = ['日', '月', '火', '水', '木', '金', '土'];
            const dayOfWeek = currentDate.getDay();

            // Header
            const header = document.createElement('div');
            header.className = 'calendar-header day-mode';
            header.innerHTML = `
                <div class="header-cell"></div>
                <div class="header-cell ${isToday(currentDate) ? 'today' : ''}">
                    ${dayNames[dayOfWeek]} ${currentDate.getDate()}日
                </div>
            `;
            calendarGrid.appendChild(header);

            // Body
            const body = document.createElement('div');
            body.className = 'calendar-body day-mode';

            // Time column
            const timeColumn = document.createElement('div');
            timeColumn.className = 'time-column';
            for (let h = 8; h <= 18; h++) {
                const slot = document.createElement('div');
                slot.className = 'time-slot';
                slot.textContent = `${h}:00`;
                timeColumn.appendChild(slot);
            }
            body.appendChild(timeColumn);

            // Day column
            const dayColumns = document.createElement('div');
            dayColumns.className = 'day-columns single';
            const dayCol = document.createElement('div');
            dayCol.className = 'day-column';
            dayCol.dataset.date = currentDate.toISOString().split('T')[0];

            // Time grid lines
            const timeGrid = document.createElement('div');
            timeGrid.className = 'time-grid';
            for (let h = 8; h <= 18; h++) {
                const line = document.createElement('div');
                line.className = 'time-grid-line';
                line.dataset.hour = h;
                line.dataset.date = currentDate.toISOString().split('T')[0];
                timeGrid.appendChild(line);
            }
            dayCol.appendChild(timeGrid);
            dayColumns.appendChild(dayCol);
            body.appendChild(dayColumns);

            calendarGrid.appendChild(body);
        }

        // Week View: 7 days with time slots
        function generateWeekView() {
            const calendarGrid = document.getElementById('calendarGrid');
            const dayNames = ['日', '月', '火', '水', '木', '金', '土'];

            const startOfWeek = new Date(currentDate);
            startOfWeek.setDate(currentDate.getDate() - currentDate.getDay());

            // Header
            const header = document.createElement('div');
            header.className = 'calendar-header week-mode';
            header.innerHTML = '<div class="header-cell"></div>';

            for (let i = 0; i < 7; i++) {
                const day = new Date(startOfWeek);
                day.setDate(startOfWeek.getDate() + i);
                const cell = document.createElement('div');
                cell.className = `header-cell ${isToday(day) ? 'today' : ''}`;
                cell.textContent = `${dayNames[i]} ${day.getDate()}日`;
                header.appendChild(cell);
            }
            calendarGrid.appendChild(header);

            // Body
            const body = document.createElement('div');
            body.className = 'calendar-body week-mode';

            // Time column
            const timeColumn = document.createElement('div');
            timeColumn.className = 'time-column';
            for (let h = 8; h <= 18; h++) {
                const slot = document.createElement('div');
                slot.className = 'time-slot';
                slot.textContent = `${h}:00`;
                timeColumn.appendChild(slot);
            }
            body.appendChild(timeColumn);

            // Day columns
            const dayColumns = document.createElement('div');
            dayColumns.className = 'day-columns week';

            for (let i = 0; i < 7; i++) {
                const day = new Date(startOfWeek);
                day.setDate(startOfWeek.getDate() + i);
                const dayCol = document.createElement('div');
                dayCol.className = 'day-column';
                dayCol.dataset.date = day.toISOString().split('T')[0];

                // Time grid lines
                const timeGrid = document.createElement('div');
                timeGrid.className = 'time-grid';
                for (let h = 8; h <= 18; h++) {
                    const line = document.createElement('div');
                    line.className = 'time-grid-line';
                    line.dataset.hour = h;
                    line.dataset.date = day.toISOString().split('T')[0];
                    timeGrid.appendChild(line);
                }
                dayCol.appendChild(timeGrid);
                dayColumns.appendChild(dayCol);
            }

            body.appendChild(dayColumns);
            calendarGrid.appendChild(body);
        }

        // Month View: Full month grid
        function generateMonthView() {
            const calendarGrid = document.getElementById('calendarGrid');
            const dayNames = ['日', '月', '火', '水', '木', '金', '土'];

            // Header
            const header = document.createElement('div');
            header.className = 'calendar-header month-mode';
            dayNames.forEach(name => {
                const cell = document.createElement('div');
                cell.className = 'header-cell';
                cell.textContent = name;
                header.appendChild(cell);
            });
            calendarGrid.appendChild(header);

            // Body
            const body = document.createElement('div');
            body.className = 'calendar-body month-mode';

            // Get first and last day of month
            const year = currentDate.getFullYear();
            const month = currentDate.getMonth();
            const firstDay = new Date(year, month, 1);
            const lastDay = new Date(year, month + 1, 0);
            const startDay = new Date(firstDay);
            startDay.setDate(startDay.getDate() - startDay.getDay()); // Start from Sunday

            // Generate cells (6 weeks)
            for (let week = 0; week < 6; week++) {
                for (let day = 0; day < 7; day++) {
                    const cellDate = new Date(startDay);
                    cellDate.setDate(startDay.getDate() + (week * 7) + day);

                    const cell = document.createElement('div');
                    cell.className = 'month-cell';
                    cell.dataset.date = cellDate.toISOString().split('T')[0];

                    if (cellDate.getMonth() !== month) {
                        cell.classList.add('other-month');
                    }
                    if (isToday(cellDate)) {
                        cell.classList.add('today');
                    }

                    cell.innerHTML = `<div class="month-date">${cellDate.getDate()}</div>`;
                    body.appendChild(cell);
                }
            }

            calendarGrid.appendChild(body);
        }

        function isToday(date) {
            const today = new Date();
            return date.getDate() === today.getDate() &&
                   date.getMonth() === today.getMonth() &&
                   date.getFullYear() === today.getFullYear();
        }

        function loadScheduledTasks() {
            @foreach (var task in Model.ScheduledTasks)
            {
                var taskDate = task.作業予定日?.ToString("yyyy-MM-dd");
                if (!string.IsNullOrEmpty(taskDate))
                {
                    <text>
                    (function() {
                        const taskDate = '@taskDate';
                        const taskTitle = '@Html.Raw(task.内容?.Replace("\"", "\\\"").Replace("\n", " "))';
                        const taskId = @task.ID;
                        const startTimeStr = '@(task.開始時刻?.ToString(@"hh\:mm") ?? "09:00")';
                        const endTimeStr = '@(task.終了時刻?.ToString(@"hh\:mm") ?? "10:00")';
                        const isImportant = @(task.重要タスクFLG == true ? "true" : "false");

                        if (viewMode === 'month') {
                            // Month view: simple task list
                            const cell = document.querySelector(`.month-cell[data-date="${taskDate}"]`);
                            if (cell) {
                                const taskEl = document.createElement('div');
                                taskEl.className = 'calendar-task' + (isImportant ? ' important' : '');
                                taskEl.textContent = taskTitle;
                                taskEl.onclick = () => editTask(taskId);
                                taskEl.style.position = 'relative';
                                taskEl.style.marginTop = '2px';
                                cell.appendChild(taskEl);
                            }
                        } else {
                            // Day/Week view: time-based positioning
                            const dayCol = document.querySelector(`.day-column[data-date="${taskDate}"]`);
                            if (dayCol) {
                                const taskEl = document.createElement('div');
                                taskEl.className = 'calendar-task' + (isImportant ? ' important' : '');
                                taskEl.textContent = taskTitle;
                                taskEl.onclick = () => editTask(taskId);
                                taskEl.dataset.taskId = taskId;
                                taskEl.draggable = true;

                                // 開始時刻と終了時刻から位置と高さを計算
                                const startParts = startTimeStr.split(':');
                                const endParts = endTimeStr.split(':');
                                const startHour = parseInt(startParts[0]) + parseInt(startParts[1]) / 60;
                                const endHour = parseInt(endParts[0]) + parseInt(endParts[1]) / 60;

                                // 8:00を基準にピクセル位置を計算（1時間 = 60px）
                                const topPx = (startHour - 8) * 60;
                                const heightPx = Math.max((endHour - startHour) * 60, 30); // 最小30px

                                taskEl.style.top = topPx + 'px';
                                taskEl.style.height = heightPx + 'px';

                                dayCol.appendChild(taskEl);
                            }
                        }
                    })();
                    </text>
                }
            }
        }

        // Navigation functions
        function navigateDate(direction) {
            if (viewMode === 'day') {
                currentDate.setDate(currentDate.getDate() + direction);
            } else if (viewMode === 'week') {
                currentDate.setDate(currentDate.getDate() + (direction * 7));
            } else if (viewMode === 'month') {
                currentDate.setMonth(currentDate.getMonth() + direction);
            }
            updateDateDisplay();
            reloadPage();
        }

        function updateDateDisplay() {
            const display = document.getElementById('currentDateDisplay');
            if (viewMode === 'day') {
                display.textContent = `${currentDate.getFullYear()}年${currentDate.getMonth() + 1}月${currentDate.getDate()}日`;
            } else if (viewMode === 'week') {
                const startOfWeek = new Date(currentDate);
                startOfWeek.setDate(currentDate.getDate() - currentDate.getDay());
                const endOfWeek = new Date(startOfWeek);
                endOfWeek.setDate(startOfWeek.getDate() + 6);
                display.textContent = `${startOfWeek.getFullYear()}年${startOfWeek.getMonth() + 1}月${startOfWeek.getDate()}日 - ${endOfWeek.getMonth() + 1}月${endOfWeek.getDate()}日`;
            } else if (viewMode === 'month') {
                display.textContent = `${currentDate.getFullYear()}年 ${currentDate.getMonth() + 1}月`;
            }
        }

        // View mode change
        $('input[name="viewMode"]').change(function() {
            viewMode = this.value;
            reloadPage();
        });

        // User change
        function changeUser() {
            selectedUserCode = document.getElementById('userSelector').value;
            reloadPage();
        }

        function reloadPage() {
            const url = new URL(window.location);
            url.searchParams.set('userCode', selectedUserCode);
            url.searchParams.set('viewMode', viewMode);
            url.searchParams.set('date', currentDate.toISOString().split('T')[0]);
            window.location.href = url.toString();
        }

        // Task functions
        function addNewTask() {
            clearTaskForm();
            $('#taskModalLabel').text('新しいタスクを追加');
            $('#deleteTaskBtn').hide();
            $('#taskModal').modal('show');
        }

        function editTask(taskId) {
            $.ajax({
                url: '/Todo/GetTask',
                method: 'GET',
                data: { id: taskId },
                success: function(response) {
                    if (response.success) {
                        populateTaskForm(response.task);
                        $('#taskModalLabel').text('タスクを編集');
                        $('#deleteTaskBtn').show();
                        $('#taskModal').modal('show');
                    } else {
                        alert('エラー: ' + response.message);
                    }
                },
                error: function() {
                    alert('通信エラーが発生しました');
                }
            });
        }

        function clearTaskForm() {
            $('#taskForm')[0].reset();
            $('#taskId').val('');
            $('#assignee').val('@Model.CurrentUserCode');
            $('#startTime').val('');
            $('#endTime').val('');
            $('#scheduledDate').val('');
            $('#importantTask').prop('checked', false);
        }

        function populateTaskForm(task) {
            $('#taskId').val(task.id);
            $('#taskContent').val(task.内容);
            $('#customerCode').val(task.取引先コード);
            $('#customerName').val(task.取引先名);
            $('#estimatedHours').val(task.見積工数);
            $('#actualHours').val(task.実績工数);
            $('#maintenanceHours').val(task.保守工数);

            if (task.作業予定日) {
                const date = new Date(task.作業予定日);
                const year = date.getFullYear();
                const month = String(date.getMonth() + 1).padStart(2, '0');
                const day = String(date.getDate()).padStart(2, '0');
                $('#scheduledDate').val(`${year}-${month}-${day}`);
            }

            if (task.修正完了日) {
                const date = new Date(task.修正完了日);
                const year = date.getFullYear();
                const month = String(date.getMonth() + 1).padStart(2, '0');
                const day = String(date.getDate()).padStart(2, '0');
                $('#completionDate').val(`${year}-${month}-${day}`);
            }

            // 開始時刻と終了時刻を設定
            if (task.開始時刻) {
                // TimeSpan形式（"HH:mm:ss"）から "HH:mm" に変換
                const timeParts = task.開始時刻.split(':');
                if (timeParts.length >= 2) {
                    $('#startTime').val(`${timeParts[0].padStart(2, '0')}:${timeParts[1].padStart(2, '0')}`);
                }
            }

            if (task.終了時刻) {
                const timeParts = task.終了時刻.split(':');
                if (timeParts.length >= 2) {
                    $('#endTime').val(`${timeParts[0].padStart(2, '0')}:${timeParts[1].padStart(2, '0')}`);
                }
            }

            $('#requester').val(task.起案者);
            $('#assignee').val(task.担当者コード);
            $('#remarks').val(task.備考);
            $('#importantTask').prop('checked', task.重要タスクFLG === true);
        }

        function saveTask() {
            if (!$('#taskContent').val().trim()) {
                alert('内容を入力してください');
                return;
            }

            const formData = {};
            $('#taskForm').serializeArray().forEach(function(item) {
                if (item.value !== '') {
                    if (item.name === 'ID') {
                        formData[item.name] = parseInt(item.value) || null;
                    } else if (item.name === '見積工数' || item.name === '実績工数' || item.name === '保守工数') {
                        formData[item.name] = parseFloat(item.value) || null;
                    } else if (item.name === '作業予定日' || item.name === '修正完了日') {
                        formData[item.name] = item.value ? new Date(item.value).toISOString() : null;
                    } else if (item.name === '開始時刻' || item.name === '終了時刻') {
                        // 時刻を "HH:mm:ss" 形式に変換
                        formData[item.name] = item.value ? item.value + ':00' : null;
                    } else if (item.name === '担当者') {
                        // 担当者フィールドは担当者コードを設定
                        formData['担当者コード'] = item.value;
                    } else {
                        formData[item.name] = item.value;
                    }
                }
            });

            // チェックボックスの値を別途取得（serializeArrayには含まれないため）
            formData['重要タスクFLG'] = $('#importantTask').is(':checked');

            const isEdit = formData.ID && formData.ID > 0;
            const url = isEdit ? '/Todo/UpdateTask' : '/Todo/CreateTask';

            $.ajax({
                url: url,
                method: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(formData),
                success: function(response) {
                    if (response.success) {
                        $('#taskModal').modal('hide');
                        location.reload();
                    } else {
                        alert('エラー: ' + response.message);
                    }
                },
                error: function() {
                    alert('通信エラーが発生しました');
                }
            });
        }

        function deleteTask() {
            const taskId = $('#taskId').val();
            if (!taskId) return;

            if (confirm('本当にこのタスクを削除しますか？')) {
                $.ajax({
                    url: '/Todo/DeleteTask',
                    method: 'POST',
                    contentType: 'application/json',
                    data: JSON.stringify({ taskId: parseInt(taskId) }),
                    success: function(response) {
                        if (response.success) {
                            $('#taskModal').modal('hide');
                            location.reload();
                        } else {
                            alert('エラー: ' + response.message);
                        }
                    },
                    error: function() {
                        alert('通信エラーが発生しました');
                    }
                });
            }
        }

        // Drag and Drop functionality
        function setupDragAndDrop() {
            // Drag from unscheduled tasks
            $(document).on('dragstart', '.task-card', function(e) {
                e.originalEvent.dataTransfer.setData('text/plain', $(this).data('task-id'));
            });

            // Drag from calendar
            $(document).on('dragstart', '.calendar-task', function(e) {
                e.originalEvent.dataTransfer.setData('text/plain', $(this).data('task-id'));
            });

            // Drag over time grid line (日/週ビュー)
            $(document).on('dragover', '.time-grid-line', function(e) {
                e.preventDefault();
                e.stopPropagation();
                $('.time-grid-line').removeClass('drop-zone');
                $(this).addClass('drop-zone');
            });

            $(document).on('dragleave', '.time-grid-line', function(e) {
                $(this).removeClass('drop-zone');
            });

            // Drop on time grid line (日/週ビュー)
            $(document).on('drop', '.time-grid-line', function(e) {
                e.preventDefault();
                e.stopPropagation();
                $(this).removeClass('drop-zone');

                const taskId = e.originalEvent.dataTransfer.getData('text/plain');
                const dropDate = $(this).data('date');
                const dropHour = parseInt($(this).data('hour'));

                const startTime = String(dropHour).padStart(2, '0') + ':00:00';
                const endTime = String(dropHour + 1).padStart(2, '0') + ':00:00';

                updateTaskDate(taskId, dropDate, startTime, endTime);
            });

            // Drag over month cell (月ビュー)
            $(document).on('dragover', '.month-cell', function(e) {
                e.preventDefault();
                $(this).addClass('drop-zone');
            });

            $(document).on('dragleave', '.month-cell', function(e) {
                $(this).removeClass('drop-zone');
            });

            // Drop on month cell (月ビュー)
            $(document).on('drop', '.month-cell', function(e) {
                e.preventDefault();
                $(this).removeClass('drop-zone');

                const taskId = e.originalEvent.dataTransfer.getData('text/plain');
                const dropDate = $(this).data('date');

                // 月ビューの場合はデフォルトで9:00-10:00
                const startTime = '09:00:00';
                const endTime = '10:00:00';

                updateTaskDate(taskId, dropDate, startTime, endTime);
            });

            // Drag over unscheduled tasks area (未定タスクエリア)
            $(document).on('dragover', '.unscheduled-tasks', function(e) {
                e.preventDefault();
                $(this).addClass('drop-zone');
            });

            $(document).on('dragleave', '.unscheduled-tasks', function(e) {
                $(this).removeClass('drop-zone');
            });

            // Drop on unscheduled tasks area (予定を未定に戻す)
            $(document).on('drop', '.unscheduled-tasks', function(e) {
                e.preventDefault();
                $(this).removeClass('drop-zone');

                const taskId = e.originalEvent.dataTransfer.getData('text/plain');

                // 日付と時刻をnullに設定して未定に戻す
                unscheduleTask(taskId);
            });
        }

        function unscheduleTask(taskId) {
            $.ajax({
                url: '/Todo/UpdateTaskDate',
                method: 'POST',
                contentType: 'application/json',
                data: JSON.stringify({
                    taskId: parseInt(taskId),
                    date: null,
                    startTime: null,
                    endTime: null
                }),
                success: function(response) {
                    if (response.success) {
                        location.reload();
                    } else {
                        alert('エラー: ' + response.message);
                    }
                },
                error: function() {
                    alert('通信エラーが発生しました');
                }
            });
        }

        function updateTaskDate(taskId, date, startTime, endTime) {
            $.ajax({
                url: '/Todo/UpdateTaskDate',
                method: 'POST',
                contentType: 'application/json',
                data: JSON.stringify({
                    taskId: parseInt(taskId),
                    date: date,
                    startTime: startTime,
                    endTime: endTime
                }),
                success: function(response) {
                    if (response.success) {
                        location.reload();
                    } else {
                        alert('エラー: ' + response.message);
                    }
                },
                error: function() {
                    alert('通信エラーが発生しました');
                }
            });
        }
    </script>
</body>
</html>